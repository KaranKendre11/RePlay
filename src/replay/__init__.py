"""RePlay — record-once, replay-many computer-use automation.

An LLM discovers how to complete a goal against a live application surface.
The successful run is distilled into a typed, versioned capability artifact.
That artifact then replays deterministically, with no model in the decision loop.
"""

__version__ = "0.1.0"
